export const Dashboard = () => {
  // const { name, email, phone, login } = data.data.results[0];
  return (
    <div>
      {/* <p>ID: {login.uuid}</p>
      <p>First name: {name.first}</p>
      <p>Last name: {name.last}</p>
      <p>Email: {email}</p>
      <p>Phone: {phone}</p> */}
    </div>
  );
};
