import { Avatar } from '@mui/material';

export const Test = () => {
  return (
    <div>
      <Avatar sx={{ width: 60, height: 60, textAlign: 'center' }}>
        Test mui
      </Avatar>
      Import like this
    </div>
  );
};
